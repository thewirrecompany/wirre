import AdminDashboard from '@/pages/admin/Dashboard';

// Render the admin dashboard UI for superadmins so pages look and behave identically.
export default function SuperadminDashboard() {
  return <AdminDashboard />;
}
